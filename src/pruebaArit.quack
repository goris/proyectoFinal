programa quack {
	var i, j num;

	libre funcion uno (num a,  num y) {
		var i num;
		i = a * 2;
		si(i < 9 + 4) {
			uno(a + 1);
		}
		i = i;
	} 

	num funcion dos (num b) {
		b = b * i + j;
		regresa (b * 2);
	}

	i = 2;
	j = i * 7 - 8;
	uno(j);
	i = i + (dos(i + j));
}
