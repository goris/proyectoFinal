programa quack {
	var i, j num;

	libre funcion uno (num a) {
		var i num;
		i = a * 2;
		si(i < a + 4) {
			a= (a + 1);
		}
		i = i;
	} 

	num funcion dos (num b) {
		b = b * i + j;
		regresa (b * 2);
	}

	i = 2;
	j = i * 2 - 1;
	uno(j);
	i = i + (dos(i + j));
}
