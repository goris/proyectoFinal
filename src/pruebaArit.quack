programa quack {

	var i num;
	
	num funcion fib (num n) {
		var prevPrev, prev, result num;
		var i num;
		prevPrev = 0;
		prev = 1;
		result = 0;
		i = 2;
		
		mientras ( i <= n){
			result = prev + prevPrev;
			prevPrev = prev;
			prev = result;
			i = i + 1 ;
		}
		regresa result;
	}

	i = fib(10);
}
